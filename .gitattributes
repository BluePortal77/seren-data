# Large layered design files -> Git LFS (not served via Pages; may exceed 100MB)
*.psd filter=lfs diff=lfs merge=lfs -text
*.psb filter=lfs diff=lfs merge=lfs -text

# PNGs stay in normal Git so GitHub Pages serves them directly
*.png !filter !diff !merge text
